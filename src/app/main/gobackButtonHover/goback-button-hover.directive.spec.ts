import { GobackButtonHoverDirective } from './goback-button-hover.directive';

describe('GobackButtonHoverDirective', () => {
  it('should create an instance', () => {
    const directive = new GobackButtonHoverDirective();
    expect(directive).toBeTruthy();
  });
});
